package nim

const (
	cacheFileName        = "cache"
	cacheTempPattern     = "cache-tmp-*"
	cacheLockSuffix      = ".lock"
	cacheTTLTempPref     = "ttl-temp-"
	defaultMaxCacheBytes = 10 * 1024 * 1024
)
